# =============================================================================
# 60-Hour Forecasting System — Python 3.11 stack (excluding torch)
# Install:  pip install -r requirements.txt
# Then run: pip install -r requirements-torch.txt --index-url https://download.pytorch.org/whl/cu121
# =============================================================================

# --- Numerical core (numpy < 2.0 because pmdarima 2.0.4 not yet numpy-2 compat)
numpy>=1.26,<2.0
pandas>=2.2,<2.3
scipy>=1.11,<1.14
scikit-learn>=1.5,<1.7
joblib>=1.4
openpyxl>=3.1
pyyaml>=6.0

# --- Time-series models
statsmodels>=0.14
pmdarima>=2.0.4
prophet>=1.1.5
holidays>=0.57

# --- Boosting + HPO + explainability
xgboost>=2.1
optuna>=4.0
shap>=0.46

# --- Service layer
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
httpx>=0.27

# --- Dashboard
streamlit>=1.39
plotly>=5.24

# --- Utilities + dev
tqdm>=4.66
matplotlib>=3.8
requests>=2.32
pytest>=8.0
